[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "futurestoolkit"
version = "0.1.0"
description = "Self-built daily futures backtesting engine for CZCE products (SA/FG/CF)."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "pandas==3.0.1",
    "numpy==2.4.2",
    "matplotlib==3.10.8",
    "TA-Lib==0.7.1",
    "optuna==4.9.0",
    "scikit-learn==1.9.0",
    "scipy==1.17.1",
    "joblib==1.5.3",
]

[project.optional-dependencies]
test = ["pytest==9.1.1"]

[tool.setuptools]
packages = ["core", "datafeed", "strategies", "research"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
